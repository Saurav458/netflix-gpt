import React from 'react'
import { useDispatch } from 'react-redux'
import { addUser } from '../utils/userSlice'
import Header from './Header'
const Browse = () => {
  // const dispatch=useDispatch()
  // dispatch(addUser("pizza"))
  return (
    <div>
        <Header/>

    </div>
  )
}

export default Browse